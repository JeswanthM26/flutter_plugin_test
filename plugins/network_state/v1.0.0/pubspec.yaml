name: apz_network_state
description: "A new Flutter plugin project."
version: 1.0.0
homepage:

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: 3.32.0

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter    
  permission_handler: ^12.0.0+1
  apz_utils:
    path: C:/Users/m.jeswanth/Desktop/Flutter_Plugins/core/apz_core/utils/v1.0.0
      

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  custom_lint: ^0.7.0  
  mocktail: ^1.0.4

flutter:

  plugin:
    platforms:
      android:
        package: com.iexceed.apz_network_state
        pluginClass: ApzNetworkState
      ios:
        pluginClass: ApzNetworkState
      web:
        pluginClass: ApzNetworkState
        fileName: web/apz_network_state.dart   
