/// This file contains the enum to specify image format.
enum ImageFormat {
  /// JPEG format.
  jpeg,

  /// PNG format.
  png,
}

/// This enum is used to specify the camera type.
enum CameraDeviceSensor {
  /// rear camera.
  rear,

  /// front camera.
  front,
}
